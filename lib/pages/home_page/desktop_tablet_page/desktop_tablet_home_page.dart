import 'package:flutter/material.dart';

class DesktopTabletHomePage extends StatelessWidget {
  const DesktopTabletHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}